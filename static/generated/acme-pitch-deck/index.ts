// Auto-generated by @fdpm/zod-bridge writePluginScaffold(). DO NOT EDIT BY HAND.
// Re-run the bridge to regenerate. Hand edits will be overwritten.

import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';
import type { PluginContext, PluginManifest } from 'fdpm/plugin';

const __dirname = dirname(fileURLToPath(import.meta.url));

// Manifest: read from fdpm-plugin.json (the file system is the source of truth).
export const manifest: PluginManifest = JSON.parse(readFileSync(join(__dirname, 'fdpm-plugin.json'), 'utf8'));

// Profile: read from generated/profile.json (the bridge writes this).
const profileSnapshot = JSON.parse(readFileSync(join(__dirname, 'generated', 'profile.json'), 'utf8'));

export const profile = profileSnapshot;

// Validator for acme:AntiPattern (cap:validator entry='antiPatternValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { antiPatternValidator } from './validators.js';

// Validator for acme:Audience (cap:validator entry='audienceValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { audienceValidator } from './validators.js';

// Validator for acme:Competitor (cap:validator entry='competitorValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { competitorValidator } from './validators.js';

// Validator for acme:DataPoint (cap:validator entry='dataPointValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { dataPointValidator } from './validators.js';

// Validator for acme:Risk (cap:validator entry='riskValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { riskValidator } from './validators.js';

// Validator for acme:Slide (cap:validator entry='slideValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { slideValidator } from './validators.js';

// Validator for acme:Source (cap:validator entry='sourceValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { sourceValidator } from './validators.js';

// Validator for acme:StrategicClaim (cap:validator entry='strategicClaimValidator').
// Provide the closure by importing your bridge-call output, e.g.:
//   import { strategicClaimValidator } from './validators.js';

export function activate(ctx: PluginContext): void {
  ctx.registerProfile(profile);
  // ctx.registerValidator({
  //   target_type_id: 'acme:AntiPattern',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: antiPatternValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:Audience',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: audienceValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:Competitor',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: competitorValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:DataPoint',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: dataPointValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:Risk',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: riskValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:Slide',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: slideValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:Source',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: sourceValidator,
  // });
  // ctx.registerValidator({
  //   target_type_id: 'acme:StrategicClaim',
  //   applies_to: 'primitive',
  //   triggers: ['create', 'patch', 'replace'],
  //   fn: strategicClaimValidator,
  // });
}

// CI drift gate — assert the in-tree snapshot matches a fresh assembly.
// (Failure here means: bump the plugin version + regenerate before commit.)
// The freshly-assembled comparison is the host's job; this file only
// fails when the JSON itself is missing or malformed.
if (!profile || profile.id !== 'profile:acme-pitch-deck:0.1') {
  throw new Error('profile drift: generated/profile.json missing or wrong id');
}
