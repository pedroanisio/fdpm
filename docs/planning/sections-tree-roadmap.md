# plan-sections-tree-implementation — Roadmap

> Profile: `profile:planning:0.1` v0.1.0. Generated by `plan:RoadmapRenderer` from 15 tasks, 1 iteration, 1 breakdown.

## Iterations

### `iteration:sections-tree-v0` — sections-tree-v0 [2026-05-04 → 2026-05-15] — Land graph-derived section numbering in spec\_authoring v0.1 with a back-compat fallback, ship the codemod, and prove byte-equal output for all 9 existing build-spec-\*.ts scripts.

**Ready** (10):
- [Ready] `task:relations-order` _(Either/P0)_ 👤actor:Bot:Builder — CHG-1: Add \`order: int\` (optional, default 0) to spec:HasSection in fdpm-cli/plugins/spec\_authoring/relations.ts. No cardinality changes; add the field after the existing description block and re-export through index.ts ids module.
- [Ready] `task:renderer-dfs` _(Either/P0)_ 👤actor:Bot:Builder — CHG-2: Replace renderSections flat-filter in spec\_md.ts with a DFS rooted at the document, sorting children by \`(order, uid)\`. Introduce \`deriveNumber(path: number\[\]): string\` that joins ancestor indices with '.'.
- [Ready] `task:fallback-detection` _(Either/P0)_ 👤actor:Bot:Builder — CHG-3: Detect 'no \`order\` edges in workbook' and route through the legacy compareSectionNumbers path; emit \`info\`-level deprecation findings on mixed-mode workbooks (Section with both authored \`number\` and a derivable position).
- [Ready] `task:tests-dfs-fixture` _(Either/P0)_ 👤actor:Bot:Builder — Pure-graph fixture in spec\_md.test.ts: build a Document with three nested sections (1 → 1.1 → 1.1.1 plus a 1.2 sibling and a §2 root sibling) using only \`order\` edges; assert the rendered headings exactly.
- [Ready] `task:tests-fallback-fixture` _(Either/P0)_ 👤actor:Bot:Builder — Fallback fixture in spec\_md.test.ts: a workbook authored with the v0.0 pattern (only authored \`number\`, no \`order\` edges) renders byte-equal to its pre-renderer-change output.
- [Ready] `task:tests-mixed-mode` _(Either/P1)_ 👤actor:Bot:Builder — Mixed-mode fixture in spec\_md.test.ts: a workbook with both authored \`number\` and \`order\` edges produces the expected count of \`info\`-level deprecation findings — one per mixed Section.
- [Ready] `task:tests-determinism` _(Either/P0)_ 👤actor:Bot:Builder — Replay-determinism gate: run the same log twice through the new (order, uid) sibling ordering and assert byte-equal SHA-256. Tiebreak shuffle: insert siblings with identical \`order\` in two different insertion orders and assert the rendered output is invariant.
- [Ready] `task:codemod` _(Either/P0)_ 👤actor:Bot:Builder — CHG-4: New script fdpm-cli/scripts/migrate-section-numbers.ts. Parses build-spec-\*.ts; replaces \`number: "N"\` with \`fields: { order: N \* 10 }\` on the matching spec:HasSection; drops \`number\` from spec:Section. Sparse 10/20/30 keeps insertion O(1).
- [Ready] `task:codemod-diff-gate` _(Either/P0)_ 👤actor:Bot:Builder — Codemod self-check: per-SPEC differential — if rendered output diverges by even one byte before/after migration, the codemod refuses to write the file. Mitigates RSK-2 (codemod silent loss).
- [Ready] `task:codemod-apply` _(Either/P0)_ 👤actor:Bot:Builder — Run the codemod against all 9 existing build-spec-\*.ts scripts. Commit the migrated forms in a separate PR; verify each migrated SPEC re-renders byte-equal to its pre-codemod render.

**Backlog** (5):
- [Backlog] `task:deprecate-number-field` _(Either/P1)_ 👤actor:Bot:Builder — CHG-6: In fdpm-cli/plugins/spec\_authoring/primitives/document.ts, mark the \`number\` field on spec:Section as deprecated in its description. No structural change in v0.1; description-only.
- [Backlog] `task:lint-sparse-order` _(Either/P2)_ 👤actor:Bot:Builder — Mitigation MIT-3: add validator spec:val:section-order-sparse — emits an \`info\` finding when a sibling group has more than two ties. Documents the 10/20/30 convention without forcing it.
- [Backlog] `task:perf-baseline` _(Either/P3)_ 👤actor:Bot:Builder — Mitigation MIT-4: benchmark render time on the largest existing SPEC (SPEC-DNIS, ~120 sections); fail CI if the post-change render exceeds 2× the pre-change baseline. Mitigates RSK-4 (render-time perf regression).
- [Backlog] `task:rollout-review` _(Human/P1)_ ⏱half day 👤actor:Person:Maintainer — Human review of the migrated SPEC outputs and the codemod's diff-gate behaviour before merging the codemod-applied PR. Spot-checks the back-compat path on at least two unmigrated scripts and the post-codemod path on at least two migrated scripts.
- [Backlog] `task:spec-status-flip` _(Either/P2)_ 👤actor:Bot:Builder — Once back-compat lands and the renderer test fixtures are green, flip SPEC-SECTIONS-TREE status from Proposal to Stable in build-spec-sections-tree.ts and re-render docs/specs/SPEC-SECTIONS-TREE.md.

---

## Work Breakdown

### `wbs:sections-tree-rollout` — sections-tree-rollout _(Active)_ — Implementation rollout for SPEC-SECTIONS-TREE v0.1 — replace hand-authored \`number\` strings on spec:Section with graph-derived numbering via a new \`order: int\` field on spec:HasSection.

- [Ready] `task:relations-order` _(Either/P0)_ 👤actor:Bot:Builder — CHG-1: Add \`order: int\` (optional, default 0) to spec:HasSection in fdpm-cli/plugins/spec\_authoring/relations.ts. No cardinality changes; add the field after the existing description block and re-export through index.ts ids module.
- [Ready] `task:renderer-dfs` _(Either/P0)_ 👤actor:Bot:Builder — CHG-2: Replace renderSections flat-filter in spec\_md.ts with a DFS rooted at the document, sorting children by \`(order, uid)\`. Introduce \`deriveNumber(path: number\[\]): string\` that joins ancestor indices with '.'.
- [Ready] `task:fallback-detection` _(Either/P0)_ 👤actor:Bot:Builder — CHG-3: Detect 'no \`order\` edges in workbook' and route through the legacy compareSectionNumbers path; emit \`info\`-level deprecation findings on mixed-mode workbooks (Section with both authored \`number\` and a derivable position).
- [Ready] `task:tests-dfs-fixture` _(Either/P0)_ 👤actor:Bot:Builder — Pure-graph fixture in spec\_md.test.ts: build a Document with three nested sections (1 → 1.1 → 1.1.1 plus a 1.2 sibling and a §2 root sibling) using only \`order\` edges; assert the rendered headings exactly.
- [Ready] `task:tests-fallback-fixture` _(Either/P0)_ 👤actor:Bot:Builder — Fallback fixture in spec\_md.test.ts: a workbook authored with the v0.0 pattern (only authored \`number\`, no \`order\` edges) renders byte-equal to its pre-renderer-change output.
- [Ready] `task:tests-mixed-mode` _(Either/P1)_ 👤actor:Bot:Builder — Mixed-mode fixture in spec\_md.test.ts: a workbook with both authored \`number\` and \`order\` edges produces the expected count of \`info\`-level deprecation findings — one per mixed Section.
- [Ready] `task:tests-determinism` _(Either/P0)_ 👤actor:Bot:Builder — Replay-determinism gate: run the same log twice through the new (order, uid) sibling ordering and assert byte-equal SHA-256. Tiebreak shuffle: insert siblings with identical \`order\` in two different insertion orders and assert the rendered output is invariant.
- [Ready] `task:codemod` _(Either/P0)_ 👤actor:Bot:Builder — CHG-4: New script fdpm-cli/scripts/migrate-section-numbers.ts. Parses build-spec-\*.ts; replaces \`number: "N"\` with \`fields: { order: N \* 10 }\` on the matching spec:HasSection; drops \`number\` from spec:Section. Sparse 10/20/30 keeps insertion O(1).
- [Ready] `task:codemod-diff-gate` _(Either/P0)_ 👤actor:Bot:Builder — Codemod self-check: per-SPEC differential — if rendered output diverges by even one byte before/after migration, the codemod refuses to write the file. Mitigates RSK-2 (codemod silent loss).
- [Ready] `task:codemod-apply` _(Either/P0)_ 👤actor:Bot:Builder — Run the codemod against all 9 existing build-spec-\*.ts scripts. Commit the migrated forms in a separate PR; verify each migrated SPEC re-renders byte-equal to its pre-codemod render.
- [Backlog] `task:deprecate-number-field` _(Either/P1)_ 👤actor:Bot:Builder — CHG-6: In fdpm-cli/plugins/spec\_authoring/primitives/document.ts, mark the \`number\` field on spec:Section as deprecated in its description. No structural change in v0.1; description-only.
- [Backlog] `task:lint-sparse-order` _(Either/P2)_ 👤actor:Bot:Builder — Mitigation MIT-3: add validator spec:val:section-order-sparse — emits an \`info\` finding when a sibling group has more than two ties. Documents the 10/20/30 convention without forcing it.
- [Backlog] `task:perf-baseline` _(Either/P3)_ 👤actor:Bot:Builder — Mitigation MIT-4: benchmark render time on the largest existing SPEC (SPEC-DNIS, ~120 sections); fail CI if the post-change render exceeds 2× the pre-change baseline. Mitigates RSK-4 (render-time perf regression).
- [Backlog] `task:rollout-review` _(Human/P1)_ ⏱half day 👤actor:Person:Maintainer — Human review of the migrated SPEC outputs and the codemod's diff-gate behaviour before merging the codemod-applied PR. Spot-checks the back-compat path on at least two unmigrated scripts and the post-codemod path on at least two migrated scripts.
- [Backlog] `task:spec-status-flip` _(Either/P2)_ 👤actor:Bot:Builder — Once back-compat lands and the renderer test fixtures are green, flip SPEC-SECTIONS-TREE status from Proposal to Stable in build-spec-sections-tree.ts and re-render docs/specs/SPEC-SECTIONS-TREE.md.

---
