# plan-dnis-implementation — Roadmap

> Profile: `profile:planning:0.1` v0.1.0. Generated by `plan:RoadmapRenderer` from 16 tasks, 1 iteration, 1 breakdown.

## Iterations

### `iteration:dnis-v0-l1` — dnis-v0-l1 [2026-05-04 → 2026-05-23] — Land a validator-backed DNIS Level 1 reference implementation, then extend it to Level 2 optimistic concurrency without pretending Level 3 is specified.

**In_review** (12):
- [In_review] `task:contract-audit` _(Either/P0)_ 👤actor:Bot:Builder — Translate DNIS into an executable contract matrix: exact Level 1 scope, Level 2 additions, explicit non-goals, and the proof mapping from TV-1..TV-6 to code and docs.
- [In_review] `task:data-model` _(Either/P0)_ 👤actor:Bot:Builder — Implement the DNIS Document, Node, Operation union, and OperationResult persistence shape with the readonly and branded-field invariants preserved at runtime.
- [In_review] `task:position-engine` _(Either/P0)_ 👤actor:Bot:Builder — Implement fractional-position generation plus compaction semantics so inserts/moves stay local and compact rebalances without revision churn.
- [In_review] `task:operation-core` _(Either/P0)_ 👤actor:Bot:Builder — Implement create, edit, and move with the exact DNIS preconditions, postconditions, identity rules, and atomicity guarantees.
- [In_review] `task:operation-lineage` _(Either/P0)_ 👤actor:Bot:Builder — Implement split, merge, and retire with lineage recording, retired-node resolution compatibility, and atomic multi-node mutation semantics.
- [In_review] `task:compact-operation` _(Either/P0)_ 👤actor:Bot:Builder — Implement compact as a first-class operation that rebalances positions without bumping per-node revision or mutating audit fields.
- [In_review] `task:idempotency-log` _(Either/P0)_ 👤actor:Bot:Builder — Persist the OperationId to OperationResult map atomically with state mutation, including snapshot-on-first-apply retry semantics and payload mismatch handling.
- [In_review] `task:hashing-canonicalization` _(Either/P0)_ 👤actor:Bot:Builder — Implement document-wide hashAlgorithm selection, algo:hex encoding, and deterministic canonicalization for JSON content at minimum.
- [In_review] `task:reference-resolution` _(Either/P0)_ 👤actor:Bot:Builder — Implement the five-outcome resolver for active, retired, evolved-via-lineage, purged, and not-found, including transitive lineage walk.
- [In_review] `task:tv-l1-harness` _(Either/P0)_ 👤actor:Bot:Builder — Encode TV-1, TV-2, TV-3, TV-4, and TV-6 as executable tests, including retry snapshot behavior, split lineage, move locality, and compact audit semantics.
- [In_review] `task:level2-concurrency` _(Either/P1)_ 👤actor:Bot:Builder — Add Level 2 optimistic-concurrency enforcement: expectedRevision on single-target operations and Mode A expectedRevisions for merge.
- [In_review] `task:tv-l2-harness` _(Either/P1)_ 👤actor:Bot:Builder — Encode TV-5 as executable concurrency proof: stale writes reject cleanly, merge Mode A checks per-target revisions, and stale attempts do not mutate state.

**Backlog** (4):
- [Backlog] `task:security-privacy-boundary` _(Either/P1)_ 👤actor:Bot:Builder — Document and enforce the real trust boundary: agent auth is external, purge is operator-gated, timestamp authority is server-side, and NIDs are never treated as secrets.
- [Backlog] `task:spec-feedback-loop` _(Either/P1)_ 👤actor:Bot:Builder — Feed implementation evidence back into DNIS: mark what is now proven, tighten any ambiguous clauses discovered during coding, and keep Level 3 explicitly deferred rather than implied.
- [Backlog] `task:level3-profile` _(Human/P3)_ ⏱1 day 👤actor:Person:Maintainer — Separate design track for a future CRDT-backed Level 3 profile. No implementation claim is allowed until §10.3 becomes normative.
- [Backlog] `task:rollout-review` _(Human/P1)_ ⏱half day 👤actor:Person:Maintainer — Human review of the Level 1/2 proof surface, unresolved open questions, and whether the repo should actually ship a reference implementation or keep DNIS as spec-only.

---

## Work Breakdown

### `wbs:dnis-rollout` — dnis-rollout _(Active)_ — Implementation rollout for the Document Node Identity Specification encoded in scripts/build-spec-dnis.ts.

- [In_review] `task:contract-audit` _(Either/P0)_ 👤actor:Bot:Builder — Translate DNIS into an executable contract matrix: exact Level 1 scope, Level 2 additions, explicit non-goals, and the proof mapping from TV-1..TV-6 to code and docs.
- [In_review] `task:data-model` _(Either/P0)_ 👤actor:Bot:Builder — Implement the DNIS Document, Node, Operation union, and OperationResult persistence shape with the readonly and branded-field invariants preserved at runtime.
- [In_review] `task:position-engine` _(Either/P0)_ 👤actor:Bot:Builder — Implement fractional-position generation plus compaction semantics so inserts/moves stay local and compact rebalances without revision churn.
- [In_review] `task:operation-core` _(Either/P0)_ 👤actor:Bot:Builder — Implement create, edit, and move with the exact DNIS preconditions, postconditions, identity rules, and atomicity guarantees.
- [In_review] `task:operation-lineage` _(Either/P0)_ 👤actor:Bot:Builder — Implement split, merge, and retire with lineage recording, retired-node resolution compatibility, and atomic multi-node mutation semantics.
- [In_review] `task:compact-operation` _(Either/P0)_ 👤actor:Bot:Builder — Implement compact as a first-class operation that rebalances positions without bumping per-node revision or mutating audit fields.
- [In_review] `task:idempotency-log` _(Either/P0)_ 👤actor:Bot:Builder — Persist the OperationId to OperationResult map atomically with state mutation, including snapshot-on-first-apply retry semantics and payload mismatch handling.
- [In_review] `task:hashing-canonicalization` _(Either/P0)_ 👤actor:Bot:Builder — Implement document-wide hashAlgorithm selection, algo:hex encoding, and deterministic canonicalization for JSON content at minimum.
- [In_review] `task:reference-resolution` _(Either/P0)_ 👤actor:Bot:Builder — Implement the five-outcome resolver for active, retired, evolved-via-lineage, purged, and not-found, including transitive lineage walk.
- [In_review] `task:tv-l1-harness` _(Either/P0)_ 👤actor:Bot:Builder — Encode TV-1, TV-2, TV-3, TV-4, and TV-6 as executable tests, including retry snapshot behavior, split lineage, move locality, and compact audit semantics.
- [In_review] `task:level2-concurrency` _(Either/P1)_ 👤actor:Bot:Builder — Add Level 2 optimistic-concurrency enforcement: expectedRevision on single-target operations and Mode A expectedRevisions for merge.
- [In_review] `task:tv-l2-harness` _(Either/P1)_ 👤actor:Bot:Builder — Encode TV-5 as executable concurrency proof: stale writes reject cleanly, merge Mode A checks per-target revisions, and stale attempts do not mutate state.
- [Backlog] `task:security-privacy-boundary` _(Either/P1)_ 👤actor:Bot:Builder — Document and enforce the real trust boundary: agent auth is external, purge is operator-gated, timestamp authority is server-side, and NIDs are never treated as secrets.
- [Backlog] `task:spec-feedback-loop` _(Either/P1)_ 👤actor:Bot:Builder — Feed implementation evidence back into DNIS: mark what is now proven, tighten any ambiguous clauses discovered during coding, and keep Level 3 explicitly deferred rather than implied.
- [Backlog] `task:level3-profile` _(Human/P3)_ ⏱1 day 👤actor:Person:Maintainer — Separate design track for a future CRDT-backed Level 3 profile. No implementation claim is allowed until §10.3 becomes normative.
- [Backlog] `task:rollout-review` _(Human/P1)_ ⏱half day 👤actor:Person:Maintainer — Human review of the Level 1/2 proof surface, unresolved open questions, and whether the repo should actually ship a reference implementation or keep DNIS as spec-only.

---
