{
  "name": "@fdpm-plugins/loop-forward",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The loop-forward v2 contract as an FDPM profile: prompt templates, agents with approval-aware tool grants, bounded feedback pipelines, output contracts and run receipts. Five design-graph renderers.",
  "scripts": {
    "test": "vitest run --root ../../ tests/plugins/loop_forward",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
