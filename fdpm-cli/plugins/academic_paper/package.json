{
  "name": "@fdpm-plugins/academic-paper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Academic paper plugin — derived from schemas/academic-paper.ts via @fdpm/zod-bridge. 18 primitives covering empirical, theoretical, methodological, literary-critical, review, historical, essay, and monograph genres.",
  "scripts": {
    "bridge": "tsx scripts/run-bridge.ts",
    "bridge:check": "tsx scripts/run-bridge.ts --check",
    "typecheck": "tsc -p ../../tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "zod": "^4.0.0",
    "@fdpm/zod-bridge": "^0.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
