## What changed

Describe the user-visible problem and the behavior this pull request adds or
repairs.

## Contract impact

List affected specifications, schemas, generated artifacts, CLI commands, SDK
exports, MCP tools/resources/prompts, and compatibility or migration concerns.

## Verification

List the exact commands and results. Include artifact or generated-consumer
evidence where compilation alone is insufficient.

- [ ] A failing regression test or equivalent pre-change evidence exists.
- [ ] Focused tests pass.
- [ ] `npm run typecheck` passes.
- [ ] `npm test` passes.
- [ ] `npm run build` passes.
- [ ] Documentation and generated artifacts agree with their source of truth.
- [ ] No credentials, local artifacts, or unrelated dirty files are included.
- [ ] AI-authored material, if any, was reviewed and verified.

## Limitations

State known gaps and follow-up work. Write `None` only after checking.
